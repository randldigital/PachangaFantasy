Please address the following issues and enhancements reported by testers for the Pachanga Fantasy v1.0 app.

---

✅ Fixes and Features to Implement:

1. **Limit input length for league and match names and descriptions**

   * Apply client-side + backend validation
   * Max: 25 characters for names, 200 for descriptions
   * Show proper error message if exceeded

2. **Allow any league member to create a lineup**

   * Remove the restriction that requires being an active match participant
   * Allow lineup creation for any member as long as a match is open
   * Adjust backend permissions accordingly

3. **Add "Delete Match" button**

   * Visible to league owner only
   * Add confirmation dialog to prevent accidental deletion
   * Backend endpoint to remove match and related data
   * Invalidate relevant TanStack queries

4. **Add "Delete League" button**

   * Visible to league owner only
   * Use confirmation modal ("Are you sure? This will remove all matches, players, and stats.")
   * Backend logic to cascade delete all related data
   * Disable access for remaining users after deletion

5. **Prevent account creation with empty password**

   * Add frontend + backend validation
   * Show appropriate error if password is missing or too short

6. **Add "End Match" button (**league owner**)**

   * Action sets match status to `completed`
   * Prompts admin to input final result
   * Then unlocks stats reporting for participants
   * Send notifications/toasts to users: “Match is completed. Please submit your stats.”

---

🧪 What to do:

* Add or update relevant components under `/client/src/components/league/`
* Update API routes and Drizzle queries accordingly
* Add validation schemas with Zod for inputs
* Run all tests after changes and add new ones if needed

⚠️ Constraints:

* Maintain existing style and structure
* Do not modify logic unrelated to these specific tasks
* Keep deletions safe and confirmable to prevent data loss

Please proceed with implementation and ensure all changes are tested and integrated cleanly.
