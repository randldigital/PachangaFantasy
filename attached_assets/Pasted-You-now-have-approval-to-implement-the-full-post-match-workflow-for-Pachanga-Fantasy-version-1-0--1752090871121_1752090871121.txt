You now have approval to implement the full post-match workflow for Pachanga Fantasy version 1.0.

Your tasks are based on our confirmed analysis and must include the following features and corrections. Use the existing codebase structure, and update only the components relevant to this scope.

---

🛠️ Tasks to Implement:

1. ✅ Final Score Entry During Match Completion
- When the league creator clicks “End Match”, prompt for total final score
- Use AdminGoalValidation as a modal or step in that action
- Save the result to the match record
- Prevent proceeding without entering the score

2. ✅ Stats Submission UI for Players
- After match status is `completed`, show a “Submit My Stats” button to users who joined the match
- Create a form to enter:
  - Goals scored
  - Assists
- On submit, call the existing `POST /api/matches/:matchId/stats` endpoint
- Disable submission once stats are submitted

3. ✅ Admin Overview of Stat Submission
- Add a section for the league creator to view:
  - Which players have submitted their stats
  - Who is still pending
- Use badges or icons to indicate submission status

4. ✅ Trigger Score Calculation
- Once all match participants have submitted stats:
  - Trigger `POST /api/matches/:matchId/calculate-scores`
  - Calculate player and user points and update the leaderboards
- Add fallback: league admin can trigger manually if needed

5. ✅ UI and Workflow Integration
- MatchDetail or LeagueDetail should reflect:
  - Current match status (`open`, `completed`, etc.)
  - Stats submitted / pending for each player
  - Button visibility based on role and state
- Add toasts/notifications to inform users:
  - “Match completed, please submit your stats”
  - “Stats submitted successfully”
  - “Waiting for other players…”

---

📂 Affected Areas and File Suggestions:

Frontend:
- `client/src/components/league/EndMatchButton.tsx` → integrate AdminGoalValidation modal
- `client/src/components/league/SubmitMyStats.tsx` → new component for player stat submission
- `client/src/components/league/AdminStatsOverview.tsx` → new component to track submissions
- `client/src/pages/LeagueDetail.tsx` or `MatchDetail.tsx` → integrate buttons and state logic

Backend:
- Validate final score input in `/api/matches/:id/end`
- Use Drizzle ORM to persist `final_score` or equivalent field
- Ensure that stat submission checks `match.status === 'completed'`
- Trigger score calculation on condition or via endpoint

Shared Types:
- Update `Match` and `StatReport` types if needed

---

🧪 Testing:
- Add tests for:
  - Final score input and validation
  - Stat submission form and API call
  - Score calculation trigger
  - Admin view of pending submissions
- Run all existing tests and ensure full pass rate

---

⚠️ Constraints:
- Use only existing stack (React, Tailwind, Drizzle, Express)
- Do not refactor unrelated features
- Do not assume real-time features or WebSockets
- Use TanStack Query for invalidation and state refreshes

---

✅ Goal:
Enable a complete and intuitive post-match flow:
End match → Enter result → Players submit stats → Points calculated → Leaderboards updated

Please begin implementation.
