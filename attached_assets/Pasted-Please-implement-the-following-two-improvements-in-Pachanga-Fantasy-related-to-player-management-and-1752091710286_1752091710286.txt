Please implement the following two improvements in Pachanga Fantasy related to player management and match completion flow.

---

🛠️ Part 1 – League Owner Can Add Non-User Players

🎯 Goal:
Allow the league creator to add external players (not linked to user accounts) to the league roster.

✔️ Requirements:
- Add “Add Player” button visible only to the league owner in the LeagueDetail page
- Clicking the button opens a modal or form:
  - Player Name (max 30 characters)
  - Optional emoji avatar
- On submit, the player is saved to the league’s player list
- These players are selectable in the lineup just like user-linked players
- Prevent duplicates (based on name + league)
- Store them in the same `players` table with a `isExternal: true` flag if needed

📂 Suggested files:
- `LeagueDetail.tsx` → add button + modal
- `AddPlayerForm.tsx` (new or reused)
- `POST /api/leagues/:id/players` → ensure it accepts external players
- Update validation to differentiate user-linked vs manual players

---

🛠️ Part 2 – Improved Post-Match Completion Flow

🎯 Goal:
Ensure that after the league owner enters the final score and ends the match:
- The UI updates automatically
- All participants (including the owner) are prompted to submit their stats

✔️ Requirements:
- After match status is set to `completed`:
  - Auto-invalidate and refetch match data using TanStack Query
  - Display a toast: “Match completed – please enter your stats”
  - Show “Submit My Stats” button to all participants (including owner if they played)
  - The page must visibly update: remove “End Match”, show new state, unlock stats form

✔️ Bonus:
- If the user already submitted stats, show confirmation (“Stats submitted”)
- Disable button after submission

📂 Suggested files:
- `EndMatchButton.tsx` → trigger query invalidation on success
- `MatchDetail.tsx` → refresh and conditionally render stat submission component
- `SubmitMyStats.tsx` → visible to all participants if match.status === 'completed'
- Ensure league creator can submit stats too if they added themselves as a player

---

✅ Goal:
1. Empower league owners to manually add players that aren’t tied to user accounts.
2. Complete and synchronize the post-match flow so the UI reflects the new state and all participants can act immediately.

Please proceed with implementation and ensure it follows the current structure and access control logic.
